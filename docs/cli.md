# Command line reference

`xcp <file.xcp>` runs a file. `xcp run <file.xcp>` uses the same loader. `xcp repl` starts the interactive prompt. `xcp help` prints usage. `xcp --version` prints the release version.

A missing file, an invalid extension, or a source error returns a non-zero status.
